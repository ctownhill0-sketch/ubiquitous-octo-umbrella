// LeadStack™ — Main Dashboard Section
// Animated counters, live stats, weekly trend charts
import { motion } from "framer-motion";
import { useEffect, useState } from "react";
import {
  AreaChart, Area, BarChart, Bar, XAxis, YAxis, Tooltip,
  ResponsiveContainer, CartesianGrid
} from "recharts";
import {
  Users, Mail, TrendingUp, Flame, Calendar, DollarSign,
  ArrowUpRight, ArrowDownRight, Phone
} from "lucide-react";
import { STATS, WEEKLY_LEADS_DATA, CITY_PERFORMANCE, MOCK_LEADS } from "@/lib/mockData";

// Animated counter hook
function useCounter(target: number, duration = 1200) {
  const [value, setValue] = useState(0);
  useEffect(() => {
    let start = 0;
    const step = target / (duration / 16);
    const timer = setInterval(() => {
      start += step;
      if (start >= target) { setValue(target); clearInterval(timer); }
      else setValue(Math.floor(start));
    }, 16);
    return () => clearInterval(timer);
  }, [target, duration]);
  return value;
}

// Stat card component
function StatCard({
  label, value, sub, icon: Icon, color, prefix = "", suffix = "", delay = 0, trend
}: {
  label: string; value: number; sub?: string; icon: React.ElementType;
  color: string; prefix?: string; suffix?: string; delay?: number;
  trend?: { value: number; positive: boolean };
}) {
  const count = useCounter(value, 1200 + delay);
  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.4, delay: delay / 1000 }}
      className="rounded-lg p-4 relative overflow-hidden"
      style={{
        background: "oklch(0.13 0.025 255)",
        border: "1px solid oklch(1 0 0 / 0.07)",
        borderTop: `2px solid ${color}`,
      }}
    >
      {/* Background glow */}
      <div className="absolute top-0 right-0 w-20 h-20 rounded-full opacity-5 pointer-events-none"
        style={{ background: color, filter: "blur(20px)", transform: "translate(30%, -30%)" }} />

      <div className="flex items-start justify-between mb-3">
        <div className="w-8 h-8 rounded flex items-center justify-center"
          style={{ background: `${color}18` }}>
          <Icon size={15} style={{ color }} />
        </div>
        {trend && (
          <div className={`flex items-center gap-0.5 text-[10px] font-medium ${trend.positive ? "text-green-400" : "text-red-400"}`}>
            {trend.positive ? <ArrowUpRight size={10} /> : <ArrowDownRight size={10} />}
            {trend.value}%
          </div>
        )}
      </div>

      <div className="stat-number text-2xl font-bold mb-0.5" style={{ color: "oklch(0.92 0.008 65)" }}>
        {prefix}{count.toLocaleString()}{suffix}
      </div>
      <div className="text-xs font-medium" style={{ color: "oklch(0.55 0.015 255)" }}>{label}</div>
      {sub && <div className="text-[10px] mt-1" style={{ color }}>{sub}</div>}
    </motion.div>
  );
}

const CustomTooltip = ({ active, payload, label }: any) => {
  if (!active || !payload?.length) return null;
  return (
    <div className="rounded-lg px-3 py-2 text-xs"
      style={{ background: "oklch(0.16 0.025 255)", border: "1px solid oklch(1 0 0 / 0.12)" }}>
      <p className="font-semibold mb-1" style={{ color: "oklch(0.92 0.008 65)" }}>{label}</p>
      {payload.map((p: any) => (
        <p key={p.name} style={{ color: p.color }}>{p.name}: {p.value}</p>
      ))}
    </div>
  );
};

export default function DashboardSection() {
  const hotLeads = MOCK_LEADS.filter(l => l.scoreLabel === "Hot").slice(0, 4);

  return (
    <div className="p-6 space-y-6">
      {/* Stats Grid */}
      <div className="grid grid-cols-2 lg:grid-cols-3 xl:grid-cols-6 gap-4">
        <StatCard label="Total Leads" value={STATS.totalLeads} icon={Users}
          color="oklch(0.72 0.12 75)" sub={`+${STATS.leadsToday} today`}
          trend={{ value: 12, positive: true }} delay={0} />
        <StatCard label="With Email" value={STATS.withEmail} icon={Mail}
          color="oklch(0.55 0.10 230)" sub="69.7% coverage"
          trend={{ value: 5, positive: true }} delay={80} />
        <StatCard label="Emails Sent" value={STATS.emailsSent} icon={TrendingUp}
          color="oklch(0.65 0.18 145)" sub={`${STATS.emailsToday} today`}
          trend={{ value: 8, positive: true }} delay={160} />
        <StatCard label="Open Rate" value={Math.round(STATS.openRate)} icon={Mail}
          color="oklch(0.72 0.12 75)" suffix="%" sub="Industry avg 21%"
          trend={{ value: 3, positive: true }} delay={240} />
        <StatCard label="Hot Leads" value={STATS.hotLeads} icon={Flame}
          color="oklch(0.65 0.22 25)" sub="Ready to call"
          trend={{ value: 40, positive: true }} delay={320} />
        <StatCard label="Pipeline" value={STATS.pipelineValue} icon={DollarSign}
          color="oklch(0.72 0.12 75)" prefix="$" sub={`${STATS.meetingsBooked} meetings`}
          trend={{ value: 22, positive: true }} delay={400} />
      </div>

      {/* Charts Row */}
      <div className="grid grid-cols-1 lg:grid-cols-3 gap-4">
        {/* Weekly Trend */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.4, delay: 0.5 }}
          className="lg:col-span-2 rounded-lg p-4"
          style={{ background: "oklch(0.13 0.025 255)", border: "1px solid oklch(1 0 0 / 0.07)" }}
        >
          <div className="flex items-center justify-between mb-4">
            <div>
              <h3 className="text-sm font-semibold" style={{ color: "oklch(0.92 0.008 65)" }}>Weekly Performance</h3>
              <p className="text-[10px]" style={{ color: "oklch(0.55 0.015 255)" }}>Leads · Emails · Replies</p>
            </div>
            <div className="flex items-center gap-3 text-[10px]">
              {[
                { label: "Leads", color: "oklch(0.72 0.12 75)" },
                { label: "Emails", color: "oklch(0.55 0.10 230)" },
                { label: "Replies", color: "oklch(0.65 0.18 145)" },
              ].map(item => (
                <div key={item.label} className="flex items-center gap-1">
                  <div className="w-2 h-2 rounded-full" style={{ background: item.color }} />
                  <span style={{ color: "oklch(0.55 0.015 255)" }}>{item.label}</span>
                </div>
              ))}
            </div>
          </div>
          <ResponsiveContainer width="100%" height={180}>
            <AreaChart data={WEEKLY_LEADS_DATA} margin={{ top: 5, right: 5, bottom: 0, left: -20 }}>
              <defs>
                <linearGradient id="leadsGrad" x1="0" y1="0" x2="0" y2="1">
                  <stop offset="5%" stopColor="oklch(0.72 0.12 75)" stopOpacity={0.15} />
                  <stop offset="95%" stopColor="oklch(0.72 0.12 75)" stopOpacity={0} />
                </linearGradient>
                <linearGradient id="emailsGrad" x1="0" y1="0" x2="0" y2="1">
                  <stop offset="5%" stopColor="oklch(0.55 0.10 230)" stopOpacity={0.15} />
                  <stop offset="95%" stopColor="oklch(0.55 0.10 230)" stopOpacity={0} />
                </linearGradient>
              </defs>
              <CartesianGrid strokeDasharray="3 3" stroke="oklch(1 0 0 / 0.04)" />
              <XAxis dataKey="day" tick={{ fontSize: 10, fill: "oklch(0.45 0.015 255)" }} axisLine={false} tickLine={false} />
              <YAxis tick={{ fontSize: 10, fill: "oklch(0.45 0.015 255)" }} axisLine={false} tickLine={false} />
              <Tooltip content={<CustomTooltip />} />
              <Area type="monotone" dataKey="leads" name="Leads" stroke="oklch(0.72 0.12 75)" strokeWidth={2} fill="url(#leadsGrad)" dot={false} />
              <Area type="monotone" dataKey="emails" name="Emails" stroke="oklch(0.55 0.10 230)" strokeWidth={2} fill="url(#emailsGrad)" dot={false} />
              <Bar dataKey="replies" name="Replies" fill="oklch(0.65 0.18 145)" radius={[2, 2, 0, 0]} />
            </AreaChart>
          </ResponsiveContainer>
        </motion.div>

        {/* City Performance */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.4, delay: 0.6 }}
          className="rounded-lg p-4"
          style={{ background: "oklch(0.13 0.025 255)", border: "1px solid oklch(1 0 0 / 0.07)" }}
        >
          <div className="mb-4">
            <h3 className="text-sm font-semibold" style={{ color: "oklch(0.92 0.008 65)" }}>Top Cities</h3>
            <p className="text-[10px]" style={{ color: "oklch(0.55 0.015 255)" }}>Leads by market</p>
          </div>
          <div className="space-y-3">
            {CITY_PERFORMANCE.map((city, i) => {
              const pct = Math.round((city.leads / CITY_PERFORMANCE[0].leads) * 100);
              return (
                <div key={city.city}>
                  <div className="flex items-center justify-between mb-1">
                    <span className="text-[11px] font-medium" style={{ color: "oklch(0.80 0.008 65)" }}>
                      {city.city}
                    </span>
                    <span className="font-mono text-[11px] font-semibold" style={{ color: "oklch(0.72 0.12 75)" }}>
                      {city.leads}
                    </span>
                  </div>
                  <div className="h-1 rounded-full overflow-hidden" style={{ background: "oklch(1 0 0 / 0.06)" }}>
                    <motion.div
                      initial={{ width: 0 }}
                      animate={{ width: `${pct}%` }}
                      transition={{ duration: 0.8, delay: 0.7 + i * 0.1 }}
                      className="h-full rounded-full"
                      style={{ background: i === 0 ? "oklch(0.72 0.12 75)" : "oklch(0.72 0.12 75 / 0.5)" }}
                    />
                  </div>
                </div>
              );
            })}
          </div>
        </motion.div>
      </div>

      {/* Hot Leads + Revenue */}
      <div className="grid grid-cols-1 lg:grid-cols-3 gap-4">
        {/* Hot Leads */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.4, delay: 0.7 }}
          className="lg:col-span-2 rounded-lg p-4"
          style={{ background: "oklch(0.13 0.025 255)", border: "1px solid oklch(1 0 0 / 0.07)" }}
        >
          <div className="flex items-center justify-between mb-4">
            <div>
              <h3 className="text-sm font-semibold" style={{ color: "oklch(0.92 0.008 65)" }}>🔥 Hot Leads — Call Now</h3>
              <p className="text-[10px]" style={{ color: "oklch(0.55 0.015 255)" }}>Highest priority contacts</p>
            </div>
            <span className="text-[10px] px-2 py-1 rounded font-medium"
              style={{ background: "oklch(0.65 0.22 25 / 0.12)", color: "oklch(0.75 0.20 25)", border: "1px solid oklch(0.65 0.22 25 / 0.25)" }}>
              {hotLeads.length} leads
            </span>
          </div>
          <div className="space-y-2">
            {hotLeads.map((lead, i) => (
              <motion.div
                key={lead.id}
                initial={{ opacity: 0, x: -10 }}
                animate={{ opacity: 1, x: 0 }}
                transition={{ delay: 0.8 + i * 0.05 }}
                className="flex items-center gap-3 p-3 rounded-lg transition-colors cursor-pointer"
                style={{ background: "oklch(1 0 0 / 0.02)", border: "1px solid oklch(1 0 0 / 0.05)" }}
                onMouseEnter={e => (e.currentTarget.style.background = "oklch(1 0 0 / 0.04)")}
                onMouseLeave={e => (e.currentTarget.style.background = "oklch(1 0 0 / 0.02)")}
              >
                <div className="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold flex-shrink-0"
                  style={{ background: "oklch(0.72 0.12 75 / 0.15)", color: "oklch(0.72 0.12 75)" }}>
                  {lead.name.split(" ").map(n => n[0]).join("").slice(0, 2)}
                </div>
                <div className="flex-1 min-w-0">
                  <div className="text-xs font-semibold truncate" style={{ color: "oklch(0.92 0.008 65)" }}>{lead.name}</div>
                  <div className="text-[10px] truncate" style={{ color: "oklch(0.55 0.015 255)" }}>{lead.company} · {lead.city}</div>
                </div>
                <div className="text-right flex-shrink-0">
                  <div className="font-mono text-[11px] font-medium" style={{ color: "oklch(0.80 0.008 65)" }}>{lead.phone}</div>
                  <div className="text-[9px]" style={{ color: "oklch(0.55 0.015 255)" }}>{lead.nextAction}</div>
                </div>
                <button className="flex-shrink-0 w-7 h-7 rounded flex items-center justify-center transition-colors"
                  style={{ background: "oklch(0.65 0.18 145 / 0.12)", color: "oklch(0.72 0.16 145)" }}>
                  <Phone size={12} />
                </button>
              </motion.div>
            ))}
          </div>
        </motion.div>

        {/* Revenue Projection */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.4, delay: 0.8 }}
          className="rounded-lg p-4 flex flex-col"
          style={{
            background: "oklch(0.13 0.025 255)",
            border: "1px solid oklch(0.72 0.12 75 / 0.2)",
            borderTop: "2px solid oklch(0.72 0.12 75)",
          }}
        >
          <h3 className="text-sm font-semibold mb-1" style={{ color: "oklch(0.92 0.008 65)" }}>Revenue Projection</h3>
          <p className="text-[10px] mb-4" style={{ color: "oklch(0.55 0.015 255)" }}>Based on current close rate</p>

          <div className="flex-1 flex flex-col justify-center space-y-4">
            <div>
              <div className="text-[10px] mb-1" style={{ color: "oklch(0.55 0.015 255)" }}>This Month (Projected)</div>
              <div className="stat-number text-3xl font-bold text-gradient-gold">
                $8,200
              </div>
              <div className="text-[10px] mt-1" style={{ color: "oklch(0.65 0.18 145)" }}>↑ 22% vs last month</div>
            </div>

            <div className="section-divider pt-4 space-y-2">
              {[
                { label: "Meetings Booked", value: "6", color: "oklch(0.72 0.12 75)" },
                { label: "Close Rate", value: "33%", color: "oklch(0.65 0.18 145)" },
                { label: "Avg Deal Size", value: "$1,367", color: "oklch(0.55 0.10 230)" },
              ].map(item => (
                <div key={item.label} className="flex items-center justify-between">
                  <span className="text-[11px]" style={{ color: "oklch(0.55 0.015 255)" }}>{item.label}</span>
                  <span className="font-mono text-[11px] font-bold" style={{ color: item.color }}>{item.value}</span>
                </div>
              ))}
            </div>
          </div>
        </motion.div>
      </div>
    </div>
  );
}
